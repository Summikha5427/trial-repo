--downlaods 

check the version =>  node -v

to install a package=> npm install packageName -y

to install default the dependencies for the packages or libraries => npm install -y

to insal the packages globally => npm install -g package-packageName

to instal redux (pure rex package)package => npm install redux

Middleware:
  redux-Logger (MiddleWare)
    applyMiddleWare,Logger
    install ==> npm install redux-Logger

//fake api json-server Creation: 

npm i -g json-server

--Command to run the json server
    ===> json-server db.json
    
--Click the link provided by the json-server , if needed single account can be called using id

--run the axios after installing in the different new terminal 
 by ==> install i axios

Thunk - Middlware

    =>install : npm i redux-thunk
    import